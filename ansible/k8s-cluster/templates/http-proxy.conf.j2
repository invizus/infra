[Service]
Environment="HTTP_PROXY={{ proxy_env.http_proxy }}"
Environment="HTTPS_PROXY={{ proxy_env.http_proxy }}"
Environment="http_proxy={{ proxy_env.http_proxy }}"
Environment="https_proxy={{ proxy_env.http_proxy }}"

